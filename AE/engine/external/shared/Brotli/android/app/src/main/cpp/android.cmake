
add_subdirectory( "../../../../../temp" "temp" )
add_library( "Static-lib" INTERFACE )
target_include_directories( "Static-lib" INTERFACE "../../../../../temp" )
target_link_libraries( "Static-lib" INTERFACE "brotlicommon" "brotlidec" "brotlienc" )
target_compile_definitions( "Static-lib" INTERFACE "AE_ENABLE_BROTLI" )
if (NOT TARGET "brotlicommon")
	message( FATAL_ERROR "target 'brotlicommon' is not exists" )
endif()
if (NOT TARGET "brotlidec")
	message( FATAL_ERROR "target 'brotlidec' is not exists" )
endif()
if (NOT TARGET "brotlienc")
	message( FATAL_ERROR "target 'brotlienc' is not exists" )
endif()
