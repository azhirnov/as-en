Android service used to:
* profile apps in release version (when engine profiling is disabled).
* profile webgl apps.
* use smartphone as remote control for [ResourceEditor](../res_editor/docs/Remote.md)


## Setup

In `Main.cpp` set `c_ServerAddr` to your PC local network IP address and `c_ServerPort` to port when server is or will be started.

