if (TARGET "HuLang")
	file( GLOB SOURCES "*.*" )
	if (DEFINED ANDROID)
		add_library( "Tests.HuLang" STATIC ${SOURCES} )
	elseif (APPLE)
		add_executable( "Tests.HuLang" MACOSX_BUNDLE ${SOURCES} )
	else()
		add_executable( "Tests.HuLang" ${SOURCES} )
	endif()
	source_group( TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${SOURCES} )
	set_property( TARGET "Tests.HuLang" PROPERTY FOLDER "Engine/Tests" )
	target_link_libraries( "Tests.HuLang" PUBLIC "HuLang" )
	target_compile_definitions( "Tests.HuLang" PUBLIC AE_TEST_HULANG )
	
	add_test( NAME "Tests.HuLang" COMMAND "Tests.HuLang" )
endif()
